{
  "routes": [
    {
      "method": "GET",
      "path": "/book-series",
      "handler": "book-series.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/book-series/count",
      "handler": "book-series.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/book-series/:id",
      "handler": "book-series.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/book-series",
      "handler": "book-series.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/book-series/:id",
      "handler": "book-series.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/book-series/:id",
      "handler": "book-series.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
